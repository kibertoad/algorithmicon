/*

Difficulty: Easy (4)
Problem statement: Given a list of events with start and end dates, calculate total amount of events that
                   happen on the same day (GMT/UTC) as at least one other event
Input format: Each row consists of two numbers: first one is start of an event, second one is end of the event.
              Each number is a Unix (epoch) timestamp
Output format: Single number that is the number of events that share day with at least one other event
*/

function implementationFn(inputLines) {
  // Your implementation
  // Return result
}

module.exports = {
  implementationFn
}
