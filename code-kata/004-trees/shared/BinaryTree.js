const { BinaryTreeNode } = require('./BinaryTreeNode')

/*
A binary search tree is a tree in which every node has no more than two children.
*/

class BinaryTree {
  constructor() {
    this.rootNode = new BinaryTreeNode()
  }
}

module.exports = {
  BinaryTree
}
